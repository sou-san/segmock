[project]
name = "segmock"
version = "0.1.0"
description = "7 segment clock (segmock)"
readme = "README.md"
authors = [
    { name = "sou-san", email = "sou887867@gmail.com" }
]
requires-python = ">=3.10"
dependencies = [
    "textual>=0.85.1",
    "textual-pyfiglet==0.5.0",
]

[project.scripts]
segmock = "segmock.__main__:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
