[project]
name = "segmock"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "sou-san", email = "sou887867@gmail.com" }
]
requires-python = ">=3.13"
dependencies = []

[project.scripts]
segmock = "segmock:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
